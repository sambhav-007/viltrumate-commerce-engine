node_modules
uploads
images
package-lock.json
img
# environment secrets
.env
server/.env
client/.env

# local tooling
.claude/

# builds
client/build

# Fleet operations (real registry may point at private client manifests)
server/provisioning/fleet.json
backups/
stores/
